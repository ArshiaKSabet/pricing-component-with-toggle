import { createTheme } from "@mui/material";

export const ThemeOptions = createTheme({
  palette: {
    primary: {
      main: "#696fdd",
    },

    background: {
      default: "#ffffff",
    },
    text: {
      primary: "#ffffff",
      secondary: "#494c5f",
    },
    divider: "#b3b5c6",
  },
});
